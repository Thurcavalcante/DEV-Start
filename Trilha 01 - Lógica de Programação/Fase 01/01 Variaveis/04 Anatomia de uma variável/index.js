/*
- let é uma palavra-chave que usamos para criar uma nova variável.

- Depois do let, temos o nome da variável.

- Em seguida, temos o sinal de igual (=), que atribuiu um valor á variável.

- E, por fim, temos o valor da variável.

- Certifique-se de criar nomes descritivos para as suas variáveis.

Ex: 
idade, temperatura, nomeDoUsuario, numeroDeAlunos

let nomeDaVariavel = valor;
*/

let idade = 30;
let temperatura = 20;
lettemperatura; // ERRADO
